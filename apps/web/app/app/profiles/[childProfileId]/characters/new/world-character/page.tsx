"use client";
import Link from "next/link"; import { useParams } from "next/navigation"; import { useEffect, useState } from "react";
type Suggestion={key:string;name:string;description:string;fitReason:string};
export default function WorldCharacterPage(){const {childProfileId}=useParams<{childProfileId:string}>();const [items,setItems]=useState<Suggestion[]>([]);const [selected,setSelected]=useState<string|null>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState<string|null>(null);
async function generate(){setLoading(true);setError(null);try{const ob=await fetch("/api/onboarding").then(r=>r.json()) as {onboarding?:{householdId?:string|null}};const householdId=ob.onboarding?.householdId;if(!householdId)throw new Error("Aile alanı bulunamadı.");const r=await fetch("/api/character-creation/world-character-suggestions",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({householdId,childProfileId})});const data=await r.json() as {suggestions?:Suggestion[];error?:string};if(!r.ok||!data.suggestions)throw new Error(data.error??"Karakter önerileri üretilemedi.");setItems(data.suggestions);setSelected(null);}catch(e){setError(e instanceof Error?e.message:"Bir sorun oluştu.");}finally{setLoading(false)}}
useEffect(()=>{void generate()},[childProfileId]);return <main className="min-h-screen bg-[#f8f4ea] px-4 py-8 text-[#34281f] sm:py-12"><section className="mx-auto max-w-5xl rounded-[32px] border border-[#e4d8c7] bg-[#fffdf7] p-5 shadow-sm sm:p-10"><p className="text-xs font-black uppercase tracking-[.14em] text-[#1f7a70]">Dünyadan başlayan yol · Karakter önerileri</p><h1 className="mt-3 font-serif text-4xl font-black">Bu dünyada kim yaşayabilir?</h1><p className="mt-4 max-w-2xl leading-7 text-[#65584d]">LUMI seçtiğin dünyanın doğasına göre dört karakter fikri hazırlıyor. Sana en çok ilham vereni seç.</p>{loading?<div className="mt-10 rounded-[26px] border border-[#dfd2be] bg-white p-8 text-center font-bold">Dünyanın sakinleri hayal ediliyor…</div>:null}{error?<div className="mt-8 rounded-2xl border border-red-200 bg-red-50 p-4"><p className="font-bold text-red-800">{error}</p><button onClick={()=>void generate()} className="mt-3 rounded-xl bg-[#16786f] px-4 py-2 font-bold text-white">Tekrar dene</button></div>:null}{!loading&&!error?<div className="mt-8 grid gap-4 sm:grid-cols-2">{items.map(x=><button key={x.key} onClick={()=>setSelected(x.key)} aria-pressed={selected===x.key} className={`rounded-[26px] border p-5 text-left transition ${selected===x.key?"border-[#16786f] bg-[#edf7f3] ring-2 ring-[#16786f]/20":"border-[#dfd2be] bg-white hover:-translate-y-0.5 hover:shadow-md"}`}><h2 className="font-serif text-2xl font-black">{x.name}</h2><p className="mt-2 leading-6 text-[#65584d]">{x.description}</p><p className="mt-4 text-sm font-bold text-[#16786f]">Bu dünyaya neden uyuyor?</p><p className="mt-1 text-sm leading-6 text-[#65584d]">{x.fitReason}</p></button>)}</div>:null}<div className="mt-8 flex flex-wrap gap-3"><Link href={`/app/profiles/${encodeURIComponent(childProfileId)}/characters/new/world-feeling`} className="rounded-2xl border border-[#dfd2be] bg-white px-5 py-3 font-extrabold">← Geri</Link>{!loading&&!error?<button onClick={()=>void generate()} className="rounded-2xl border border-[#16786f] bg-white px-5 py-3 font-extrabold text-[#16786f]">Yeni öneriler üret</button>:null}<button disabled={!selected} className="rounded-2xl bg-[#16786f] px-6 py-3 font-extrabold text-white disabled:opacity-40">Bu karakterle devam et →</button></div></section></main>}
