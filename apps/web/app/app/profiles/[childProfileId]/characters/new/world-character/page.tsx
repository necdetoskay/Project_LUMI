import Link from "next/link";
export default async function WorldCharacterPage({params}:{params:Promise<{childProfileId:string}>}){const {childProfileId}=await params;return <main className="min-h-screen bg-[#f8f4ea] px-4 py-10 text-[#34281f]"><section className="mx-auto max-w-4xl rounded-[32px] border border-[#e4d8c7] bg-[#fffdf7] p-7 shadow-sm sm:p-10"><p className="text-xs font-black uppercase tracking-[0.14em] text-[#1f7a70]">Dünyadan başlayan yol · Karakter önerileri</p><h1 className="mt-3 font-serif text-4xl font-black">Bu dünyada kim yaşayabilir?</h1><p className="mt-4 max-w-2xl leading-7 text-[#65584d]">Dünya seçimin kaydedildi. Sıradaki dilimde LUMI bu seçimi gerçek model girdisi olarak kullanıp dünyaya uyumlu karakter tipleri üretecek.</p><Link href={`/app/profiles/${encodeURIComponent(childProfileId)}/characters/new/world-feeling`} className="mt-7 inline-flex rounded-2xl border border-[#dfd2be] bg-white px-5 py-3 font-extrabold">← Dünya seçimine dön</Link></section></main>}
