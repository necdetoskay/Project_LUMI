import Link from "next/link";
export default async function CharacterIdentityPage({params}:{params:Promise<{childProfileId:string}>}){const {childProfileId}=await params;return <main className="min-h-screen bg-[#f8f4ea] px-4 py-10 text-[#34281f]"><section className="mx-auto max-w-4xl rounded-[32px] border border-[#e4d8c7] bg-[#fffdf7] p-7 shadow-sm sm:p-10"><p className="text-xs font-black uppercase tracking-[.14em] text-[#1f7a70]">Karakter oluşturma · Kimlik</p><h1 className="mt-3 font-serif text-4xl font-black">Şimdi ona kim olduğunu verelim.</h1><p className="mt-4 max-w-2xl leading-7 text-[#65584d]">Karakter tipi artık draft'a kaydedildi. Sıradaki adımda mevcut dünya ve karakter seçimlerini kullanarak isim, kısa kimlik ve ayırt edici karakter özellikleri için öneriler hazırlayacağız.</p><Link href={`/app/profiles/${encodeURIComponent(childProfileId)}/characters/new/world-character`} className="mt-7 inline-flex rounded-2xl border border-[#dfd2be] bg-white px-5 py-3 font-extrabold">← Karakter seçimine dön</Link></section></main>}
