{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "turbo run clean && rimraf node_modules .turbo"
  },
  "engines": {
    "node": ">=22 <25"
  },
  "packageManager": "pnpm@<PIN_EXACT_VERSION>"
}
