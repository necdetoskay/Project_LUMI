# Sprint 42 Validation Scope

Sprint 42 closes only when the final PR head is green across the repository quality gates. The sprint intentionally separates **visual-canon contract readiness** from **provider-backed image generation completeness**. The latter must not be marked complete without a real image provider adapter, durable/referenceable asset storage, four-candidate batch generation, explicit selection persistence, tenant isolation and idempotent retry evidence.
