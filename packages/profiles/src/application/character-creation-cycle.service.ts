import { and, desc, eq } from "drizzle-orm"; import { getProfileDb } from "./db"; import { DrizzleHouseholdRepository } from "../db/repositories/drizzle/drizzle-household.repository"; import { characterCreationCycles, characterCreationSelections, childProfiles, type CharacterCreationDirection } from "../db/schema/profile"; import { AuthorizationError } from "../domain/errors";
export type WorldFeelingKey="oceanic"|"sky_islands"|"enchanted_forest"|"crystal_caverns"|"desert_ruins"|"living_city";
async function assertScope(userId:string,householdId:string,childProfileId:string){const db=getProfileDb();const household=await new DrizzleHouseholdRepository(db).findByIdForUser(householdId,userId);if(!household)throw new AuthorizationError("User is not a member of this household");const [profile]=await db.select({id:childProfiles.id}).from(childProfiles).where(and(eq(childProfiles.id,childProfileId),eq(childProfiles.householdId,householdId))).limit(1);if(!profile)throw new AuthorizationError("Child profile does not belong to this household")}
export async function getActiveCharacterCreationCycle(userId:string,householdId:string,childProfileId:string){await assertScope(userId,householdId,childProfileId);const db=getProfileDb();const [cycle]=await db.select().from(characterCreationCycles).where(and(eq(characterCreationCycles.householdId,householdId),eq(characterCreationCycles.childProfileId,childProfileId),eq(characterCreationCycles.status,"draft"))).orderBy(desc(characterCreationCycles.updatedAt)).limit(1);return cycle??null}
export async function chooseCharacterCreationDirection(userId:string,input:{householdId:string;childProfileId:string;direction:CharacterCreationDirection}){await assertScope(userId,input.householdId,input.childProfileId);const db=getProfileDb();const existing=await getActiveCharacterCreationCycle(userId,input.householdId,input.childProfileId);const nextStep=input.direction==="character_first"?"character_type":"world_feeling";const cycleId=existing?.id??crypto.randomUUID();if(existing)await db.update(characterCreationCycles).set({startDirection:input.direction,currentStep:nextStep,latestSummary:{...(existing.latestSummary??{}),startDirection:input.direction},updatedAt:new Date()}).where(eq(characterCreationCycles.id,existing.id));else await db.insert(characterCreationCycles).values({id:cycleId,childProfileId:input.childProfileId,householdId:input.householdId,startDirection:input.direction,currentStep:nextStep,latestSummary:{startDirection:input.direction}});await db.insert(characterCreationSelections).values({id:crypto.randomUUID(),cycleId,childProfileId:input.childProfileId,householdId:input.householdId,stepKey:"start",selectionKey:input.direction,selectionPayload:{direction:input.direction},selectedBy:"user"});return{id:cycleId,startDirection:input.direction,currentStep:nextStep}}
export async function chooseWorldFeeling(userId:string,input:{householdId:string;childProfileId:string;feeling:WorldFeelingKey}){await assertScope(userId,input.householdId,input.childProfileId);const db=getProfileDb();const cycle=await getActiveCharacterCreationCycle(userId,input.householdId,input.childProfileId);if(!cycle||cycle.startDirection!=="world_first")throw new Error("World-first creation cycle is required");const latestSummary={...(cycle.latestSummary??{}),worldFeeling:input.feeling};await db.update(characterCreationCycles).set({currentStep:"world_character_suggestions",latestSummary,updatedAt:new Date()}).where(eq(characterCreationCycles.id,cycle.id));await db.insert(characterCreationSelections).values({id:crypto.randomUUID(),cycleId:cycle.id,childProfileId:input.childProfileId,householdId:input.householdId,stepKey:"world_feeling",selectionKey:input.feeling,selectionPayload:{feeling:input.feeling},selectedBy:"user"});return{id:cycle.id,startDirection:cycle.startDirection,currentStep:"world_character_suggestions",latestSummary}}
export async function chooseWorldCharacterSuggestion(userId:string,input:{householdId:string;childProfileId:string;suggestion:{key:string;name:string;description:string;fitReason:string}}){await assertScope(userId,input.householdId,input.childProfileId);const db=getProfileDb();const cycle=await getActiveCharacterCreationCycle(userId,input.householdId,input.childProfileId);if(!cycle||cycle.startDirection!=="world_first")throw new Error("WORLD_FIRST_CYCLE_REQUIRED");const latestSummary={...(cycle.latestSummary??{}),characterArchetype:input.suggestion};await db.update(characterCreationCycles).set({currentStep:"character_identity",latestSummary,updatedAt:new Date()}).where(eq(characterCreationCycles.id,cycle.id));await db.insert(characterCreationSelections).values({id:crypto.randomUUID(),cycleId:cycle.id,childProfileId:input.childProfileId,householdId:input.householdId,stepKey:"world_character_suggestion",selectionKey:input.suggestion.key,selectionPayload:input.suggestion,selectedBy:"user"});return{id:cycle.id,currentStep:"character_identity",latestSummary}}
export async function chooseCharacterIdentity(userId:string,input:{householdId:string;childProfileId:string;suggestion:{key:string;name:string;identity:string;traits:[string,string,string];fitReason:string}}){await assertScope(userId,input.householdId,input.childProfileId);const db=getProfileDb();const cycle=await getActiveCharacterCreationCycle(userId,input.householdId,input.childProfileId);if(!cycle)throw new Error("CHARACTER_CREATION_CYCLE_REQUIRED");const latestSummary={...(cycle.latestSummary??{}),characterIdentity:input.suggestion};await db.update(characterCreationCycles).set({currentStep:"origin",latestSummary,updatedAt:new Date()}).where(eq(characterCreationCycles.id,cycle.id));await db.insert(characterCreationSelections).values({id:crypto.randomUUID(),cycleId:cycle.id,childProfileId:input.childProfileId,householdId:input.householdId,stepKey:"character_identity",selectionKey:input.suggestion.key,selectionPayload:input.suggestion,selectedBy:"user"});return{id:cycle.id,currentStep:"origin",latestSummary}}
