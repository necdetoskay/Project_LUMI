name: Temporary Format Context P0

on:
  push:
    branches:
      - agent/context-p0-correctness-privacy

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/context-p0-correctness-privacy
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write packages/profiles/src/application/generation-context-assembler.test.ts packages/profiles/src/application/generation-context-assembler.ts
      - name: Commit formatting
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No formatting changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/profiles/src/application/generation-context-assembler.test.ts packages/profiles/src/application/generation-context-assembler.ts
          git commit -m "style(context): format P0 hardening"
          git push origin HEAD:agent/context-p0-correctness-privacy
