name: Test Lab locale formatter once

on:
  push:
    branches:
      - feat/test-lab-locale-results

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/test-lab-locale-results
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write apps/web/app/app/settings/test-lab/onboarding-test-runner.tsx
      - name: Commit formatter output
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add apps/web/app/app/settings/test-lab/onboarding-test-runner.tsx
          git commit -m "style(test-lab): apply prettier"
          git push origin HEAD:feat/test-lab-locale-results
