name: Temporary Phase 6 Evaluation Prettier 2

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  format:
    if: github.event.pull_request.head.ref == 'agent/test-lab-phase-6-evaluation-engine'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Format evaluation files
        run: |
          pnpm exec prettier --write \
            apps/web/app/api/settings/test-lab/evaluations/route.ts \
            apps/web/lib/ai/test-lab-evaluation-judge-adapter.ts \
            packages/ai/src/db/schema/ai/test-lab-evaluation.ts \
            packages/ai/src/test-lab/application/evaluation-runner.ts \
            packages/ai/tests/evaluation-repository.test.ts \
            packages/ai/tests/evaluation-runner.test.ts \
            packages/ai/tests/integration/drizzle-evaluation.repository.test.ts
      - name: Commit exact Prettier output
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            apps/web/app/api/settings/test-lab/evaluations/route.ts \
            apps/web/lib/ai/test-lab-evaluation-judge-adapter.ts \
            packages/ai/src/db/schema/ai/test-lab-evaluation.ts \
            packages/ai/src/test-lab/application/evaluation-runner.ts \
            packages/ai/tests/evaluation-repository.test.ts \
            packages/ai/tests/evaluation-runner.test.ts \
            packages/ai/tests/integration/drizzle-evaluation.repository.test.ts
          git commit -m "style(test-lab): format evaluation engine slice"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
