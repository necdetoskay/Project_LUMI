name: ULTEF S42 Character Creation Source Contract

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  source-contract:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.7.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Web typecheck
        run: pnpm --filter @lumi/web typecheck
      - name: Web lint
        run: pnpm --filter @lumi/web lint
      - name: Run S42 source contract
        run: pnpm --filter @lumi/web exec playwright test tests/ultef-s42-character-creation-source.spec.ts --project=chromium
