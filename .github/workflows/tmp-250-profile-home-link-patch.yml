name: Temporary Phase 10 Profile Home Patch

on:
  push:
    branches:
      - fix/250-live-profile-navigation
    paths:
      - .github/workflows/tmp-250-profile-home-link-patch.yml
      - tooling/tmp-250-profile-home-link-patch.py

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout patch branch
        uses: actions/checkout@v4
        with:
          ref: fix/250-live-profile-navigation

      - name: Apply exact child-dashboard link patch
        run: python tooling/tmp-250-profile-home-link-patch.py

      - name: Remove temporary helpers
        run: rm tooling/tmp-250-profile-home-link-patch.py .github/workflows/tmp-250-profile-home-link-patch.yml

      - name: Commit patched live spec
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- apps/web/tests/e2e/long-horizon/baseline-onboarding.live.spec.ts tooling/tmp-250-profile-home-link-patch.py .github/workflows/tmp-250-profile-home-link-patch.yml
          git diff --cached --check
          git commit -m "test(live): use visible child dashboard profile link"
          git push origin HEAD:fix/250-live-profile-navigation
